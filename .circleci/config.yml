version: 2.1

jobs:
  test:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout

      - run:
          name: Install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt

      - run:
          name: Run pytest with reporter-html1
          command: |
            . venv/bin/activate
            pytest --report=pytest-report.html

      - store_artifacts:
          path: pytest-report.html
          destination: test-report

workflows:
  version: 2
  test_all:
    jobs:
      - test
